@AGENTS.md

# סטנדרטים לעבודה על הפרויקט

## הרשמה עם אימייל+סיסמה — החלטת מוצר מכוונת

`src/app/signup/page.tsx` מאפשר הרשמה גם עם אימייל+סיסמה, לא רק Google.
זו **החלטה מכוונת**, לא רגרסיה שצריך "לתקן" בחזרה ל-Google-only:

- "Confirm email" כבוי ב-Supabase כדי לאפשר כניסה מיידית בלי לחכות
  למייל אישור. המשמעות: כל אחד יכול להירשם עם כל כתובת מייל, כולל
  כתובת שהיא לא שלו, בלי אימות בעלות. Google הוא הדרך היחידה
  שמאמתת זהות בפועל.
- ההגנה בטופס האימייל+סיסמה (`/api/auth/signup-guard`) היא שדה דבש
  + הגבלת קצב בלבד — הגנה בסיסית מפני ספאם אוטומטי, **לא** CAPTCHA
  ולא אימות זהות. בוט שפונה ישירות ל-API של Supabase (המפתח
  הציבורי חשוף ממילא בצד הלקוח) עוקף את זה לגמרי — זו מגבלה ידועה
  ומקובלת, לא פרצה שצריך לסגור.
- אם בעתיד נדרש אימות זהות אמיתי, הפתרון הוא להחזיר "Confirm email"
  או לדרוש Google בלבד — לא להוסיף CAPTCHA לטופס הזה.

## סטנדרטים קבועים לכל עבודה עתידית

- **מובייל-first חובה**: כל קומפוננטה חדשה נבדקת קודם ב-375px לפני דסקטופ.
- **גובה הניווט הקבוע**: כל עמוד חדש נטען מתחת ל-`<main id="main">`
  ב-`src/app/layout.tsx`, שכבר מפצה על `--spacing-header` באופן
  גלובלי. אלמנט `sticky`/`fixed` חדש חייב offset של
  `var(--spacing-header)` (או `calc(var(--spacing-header) + Npx)`),
  אחרת הוא נתקע מתחת לניווט בגלילה — ראו את התיקון ב-`AdminShell.tsx`,
  `FilterRail.tsx` ו-`business/[slug]/page.tsx` כדוגמה.
- **טוקנים בלבד**: אין ערך צבע/רדיוס/צל ישיר ברכיב — הכל דרך `@theme`
  ב-`globals.css`, כפי שכבר מתועד ב-`DESIGN_SYSTEM.md`.
- **מיגרציות**: כל שינוי סכימה הוא קובץ מיגרציה חדש וממוספר ב-
  `supabase/migrations/`, וצריך להירשם בפועל מול הפרויקט (לא רק
  להיכתב) לפני שנחשב "גמור".
- **Core Web Vitals ו-SEO** (JSON-LD, מטא-דאטה דינמית, sitemap) הם
  חלק מהגדרת "גמור" לכל עמוד ציבורי חדש, לא שלב נפרד בסוף.
- **נגישות (WCAG 2.1 AA)** היא חלק מהגדרת "גמור", לא ביקורת נפרדת —
  הפרויקט כבר משקיע בזה (`AccessibilityToolbar`, focus-visible,
  prefers-reduced-motion) ואסור לרגרס.
